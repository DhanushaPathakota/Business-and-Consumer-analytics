<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.6.0/Chart.min.js"></script>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <title>My Chart.js Chart</title>
</head>

<%ids.forEach(function(it){%>
<div class="chart-container" style="position: relative; float: left;height:10vh; width:25vw">

    <canvas id="myChart<%=it%>"></canvas>

</div>

<canvas id="myChart<%=it%>" width="100px" height="200px"></canvas>
  <%});%>
<script>
  //  var lengthnew=<%=lengthnew%>;
  <%ids.forEach(function(it,i){%>
var ctx = document.getElementById('myChart<%=it%>').getContext('2d');

//for(i=0;i<6;i=i+1)
//{
var myChart = new Chart(ctx, {
    type: 'bar',
    data: {
        labels: ['1star','2star','3star','4star','5star'],
        datasets: [{
            label: 'no of stars for employee: '+<%=it%>,
            data: [<%= rate.slice((i*5),((i*5)+5))%>],

            backgroundColor: [
                'rgba(255, 99, 132, 0.2)',
                'rgba(54, 162, 235, 0.2)',
                'rgba(255, 206, 86, 0.2)',
                'rgba(75, 192, 192, 0.2)',
                'rgba(153, 102, 255, 0.2)',
                'rgba(255, 159, 64, 0.2)'
            ],
            borderColor: [
                'rgba(255, 99, 132, 1)',
                'rgba(54, 162, 235, 1)',
                'rgba(255, 206, 86, 1)',
                'rgba(75, 192, 192, 1)',
                'rgba(153, 102, 255, 1)',
                'rgba(255, 159, 64, 1)'
            ],
            borderWidth: 1
        }]
    },
    options: {
        scales: {
            yAxes: [{
                ticks: {
                    beginAtZero: true
                }
            }]
        }
    }
});
//}
  <%});%>
</script>



</html>
